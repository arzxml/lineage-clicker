# Drop your PNG template images here.
# Each file's stem becomes the template name used in TemplateMatcher.find().
#
# Examples:
#   target_hp_bar.png   → matcher.find(frame, "target_hp_bar")
#   attack_button.png   → matcher.find(frame, "attack_button")
#   loot_bag.png        → matcher.find(frame, "loot_bag")
#
# Capture snippets from the game with the screen capture tool or any
# screenshot utility. Higher quality / more unique crops = better matching.
